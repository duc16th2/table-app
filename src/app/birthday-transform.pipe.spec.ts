import { BirthdayTransformPipe } from './birthday-transform.pipe';

describe('BirthdayTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new BirthdayTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
